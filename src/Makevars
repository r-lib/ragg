PKG_CXXFLAGS = -I./agg/include `pkg-config --cflags freetype2 fontconfig`
PKG_LIBS = -L./agg/src `pkg-config --libs freetype2 fontconfig`

AGG_SOURCES = $(wildcard agg/src/*.cpp)
OWN_SOURCES = $(wildcard *.cpp)
SOURCES = $(AGG_SOURCES) $(OWN_SOURCES)
OBJECTS = $(SOURCES:.cpp=.o)
